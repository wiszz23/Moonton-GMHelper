# GM助手 Chrome 扩展 — 自托管更新指南

## 概述

通过 GitHub Pages 搭建自托管更新服务器，实现插件自动更新。用户首次手动安装后，后续版本更新浏览器会自动弹出提示，无需手动下载替换。

## 名词解释

| 术语 | 说明 |
|---|---|
| 扩展 ID | Chrome 根据公钥生成的唯一标识，固定不变，用于标识此扩展 |
| update_url | 扩展检查更新的 XML 文件地址，Chrome 会定期访问此地址 |
| updates.xml | 更新清单文件，包含最新版本号和 zip 包下载地址 |
| GMHelper.zip | 插件打包文件，包含所有源码和资源 |

## 必要信息（已生成）

```
扩展 ID:    VasAx2Lsjsb6LetT7XJc2Q
当前版本:   3.15
manifest.json 中已包含 key 和 update_url
```

## 操作流程

### 第一阶段：初始化（仅需执行一次）

#### 1. 创建 GitHub 仓库

1. 登录 GitHub，点击右上角 **+ → New repository**
2. Repository name 填写 `gm-helper-updates`（名字可自定义）
3. 选择 **Public**（GitHub Pages 必须 Public）
4. 点击 **Create repository**

#### 2. 上传扩展文件到仓库

进入仓库页面，点击 **Add file → Upload files**，将以下文件全部拖入上传：

```
gm-helper-updates/（仓库根目录）
├── updates.xml          ← 已在本地生成
├── GMHelper.zip        ← 需要重新打包（见下方打包步骤）
├── manifest.json        ← 已包含 key 和 update_url
├── content_script.js
├── panel.js
├── panel.html
├── popup.html
├── popup.js
├── background.js
├── styles.css
├── commands.json
├── devtools.html
└── ...（GMHelper 文件夹下所有文件）
```

#### 3. 开启 GitHub Pages

1. 进入仓库 **Settings → Pages**
2. **Source** 选择 **Deploy from a branch**
3. **Branch** 选择 **main**，目录选 `/ (root)`
4. 点击 **Save**

等待 1-2 分钟，页面顶部会出现绿色提示：
`Your site is published at https://wiszz23.github.io/Moonton-GMHelper/`

#### 4. 确认文件可访问

在浏览器打开以下地址，确认都能正常显示/下载：

- `https://wiszz23.github.io/Moonton-GMHelper/updates.xml`
- `https://wiszz23.github.io/Moonton-GMHelper/GMHelper.zip`

#### 5. 更新 manifest.json 和 updates.xml 中的 URL

将本地 `GMHelper/manifest.json` 和 `GMHelper/updates.xml` 中的占位符替换为实际地址：

**manifest.json** 修改：
```json
{
  "key": "",
  "update_url": "https://wiszz23.github.io/Moonton-GMHelper/updates.xml"
}
```

**updates.xml** 修改：
```xml
<?xml version='1.0' encoding='UTF-8'?>
<gupdate xmlns='http://www.google.com/update2/response' protocol='2.0'>
  <app appid='VasAx2Lsjsb6LetT7XJc2Q'>
    <updatecheck
      codebase='https://wiszz23.github.io/Moonton-GMHelper/GMHelper.zip'
      version='3.15' />
  </app>
</gupdate>
```

将修改后的 `manifest.json` 同步上传到 GitHub 仓库（覆盖旧文件）。

#### 6. 用户首次安装

1. 用户打开 Chrome，地址栏输入 `chrome://extensions/`
2. 右上角开启 **开发者模式**
3. 点击 **加载已解压的扩展程序**
4. 选择本地的 `GMHelper` 文件夹

> 注意：用户加载的是本地文件夹（不是 zip），这个文件夹里已经包含更新地址，之后 Chrome 会自动访问该地址检查更新。

---

### 第二阶段：日常更新（每次发布新版本）

#### 1. 修改本地文件版本号

编辑 `GMHelper/manifest.json`：

```json
"version": "3.16"
```

#### 2. 打包 zip

进入 `GMHelper` 文件夹，将**所有文件**打包为 `GMHelper.zip`（注意是 zip，不是 rar）

Windows 打包方法：
- 选中所有文件 → 右键 → 发送到 → 压缩(zipped)文件夹 → 重命名为 `GMHelper.zip`

#### 3. 上传到 GitHub

将以下两个文件上传到 GitHub 仓库：

- `GMHelper/GMHelper.zip` → 覆盖仓库中的 `GMHelper.zip`
- `GMHelper/manifest.json` → 覆盖仓库中的 `manifest.json`

#### 4. 修改 updates.xml

在 GitHub 仓库中编辑 `updates.xml`，更新版本号：

```xml
<?xml version='1.0' encoding='UTF-8'?>
<gupdate xmlns='http://www.google.com/update2/response' protocol='2.0'>
  <app appid='VasAx2Lsjsb6LetT7XJc2Q'>
    <updatecheck
      codebase='https://wiszz23.github.io/Moonton-GMHelper/GMHelper.zip'
      version='3.16' />
  </app>
</gupdate>
```

#### 5. 完成

用户打开 Chrome 浏览器时，Chrome 会自动检测到新版本，弹出更新提示，点击即可自动更新。

---

## 注意事项

1. **版本号必须递增**：每次更新必须比上次大，Chrome 通过版本号判断是否需要更新
2. **zip 内容必须包含 manifest.json**：Chrome 从 zip 内的 manifest.json 读取版本号
3. **仓库必须 Public**：GitHub Pages 需要 Public 仓库才能开启
4. **扩展 ID 固定不变**：只要不更换 key，扩展 ID 永远为 `VasAx2Lsjsb6LetT7XJc2Q`
5. **用户首次必须手动安装**：自动更新仅对已安装的扩展有效，无法自动推送给未安装的用户

## 常见问题

**Q: 用户提示"扩展可能已损坏"？**
A: 检查 zip 包内的 manifest.json 是否放在根目录，不能嵌套在子文件夹中。

**Q: 更新后用户没收到提示？**
A: Chrome 检查更新间隔不固定，可能需要等待数小时。可以让用户在 `chrome://extensions/` 页面点击右上角"更新"按钮强制检查。

**Q: 可以用 Gitee Pages 代替吗？**
A: 可以，国内访问更快。将 `github.io` 替换为 `gitee.io` 地址即可，流程完全相同。

**Q: 能自动化打包和更新吗？**
A: 可以。用 GitHub Actions 监听 `manifest.json` 的 push 事件，自动打包 zip、自动更新 `updates.xml`。如需此方案请告知。
