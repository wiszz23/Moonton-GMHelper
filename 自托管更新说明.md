# GM助手 Chrome 扩展 — 自托管更新指南

## 概述

通过 GitHub Pages + GitHub Actions 实现插件自动更新。用户首次手动安装后，后续版本更新浏览器会自动弹出提示，无需手动打包上传。

## 名词解释

| 术语 | 说明 |
|---|---|
| 扩展 ID | Chrome 根据公钥生成的唯一标识，固定不变 |
| update_url | 扩展检查更新的 XML 文件地址，Chrome 会定期访问此地址 |
| updates.xml | 更新清单文件，包含最新版本号和 zip 包下载地址 |
| GMHelper.zip | 插件打包文件，包含所有源码和资源 |
| GitHub Actions | GitHub 内置的自动化工具，检测到代码 push 后自动执行打包部署 |
| gh-pages | 专用分支，存放发布到 GitHub Pages 的文件 |

## 必要信息

```
扩展 ID:    VasAx2Lsjsb6LetT7XJc2Q
Pages 地址: https://wiszz23.github.io/Moonton-GMHelper/
仓库地址:   https://github.com/wiszz23/Moonton-GMHelper
```

## 工作流程

```
你改代码 → git push 到 master 分支
    ↓ 自动触发 GitHub Actions
workflow 自动：
  1. 打包扩展文件为 GMHelper.zip
  2. 更新 updates.xml 版本号
  3. 推送到 gh-pages 分支
    ↓ GitHub Pages 自动发布（1-2分钟）
用户打开 Chrome → 自动检测到新版本 → 弹出更新提示
```

---

## 初始化（仅需执行一次）

### 1. 确认仓库结构

仓库 `wiszz23/Moonton-GMHelper` 需要两个分支：

| 分支 | 用途 |
|---|---|
| `master` | 存放源代码，包含 GitHub Actions workflow |
| `gh-pages` | 由 workflow 自动生成，存放发布文件 |

### 2. 开启 GitHub Pages

1. 进入仓库 **Settings → Pages**
2. **Source** 选择 **Deploy from a branch**
3. **Branch** 选择 **gh-pages**，目录选 `/ (root)`
4. 点击 **Save**

等待 1-2 分钟，Pages 激活后即可访问：
`https://wiszz23.github.io/Moonton-GMHelper/`

### 3. 确认 workflow 文件存在

确保 `.github/workflows/release.yml` 文件在 master 分支中，内容包含 GitHub Actions 自动化流程。

### 4. 用户首次安装

1. 用户打开 Chrome，地址栏输入 `chrome://extensions/`
2. 右上角开启 **开发者模式**
3. 点击 **加载已解压的扩展程序**
4. 选择本地的 `GMHelper` 文件夹

> 注意：用户加载的是本地文件夹，这个文件夹里已包含 `update_url`，之后 Chrome 会自动访问该地址检查更新。

---

## 日常更新（每次发布新版本）

### 操作步骤

1. **修改代码**（如 `panel.js`、`content_script.js` 等）
2. **修改版本号**：编辑 `manifest.json`，将 `"version"` 改为更大版本（如 `3.16`）
3. **git push**：推送到 master 分支
4. **等待**：去 GitHub Actions 页面查看 workflow 运行结果（约 1 分钟）
5. **完成**：用户打开 Chrome 时自动弹出更新提示

### 无需手动操作（开发者视角）

- 不需要手动打包 zip
- 不需要手动上传文件到 GitHub
- 不需要手动修改 updates.xml 版本号（workflow 自动更新）
- workflow 全自动完成

### 其他用户如何接收更新（使用者视角）

用户只需**首次手动安装一次**，之后无需任何操作：

1. **首次安装**：加载本地扩展文件夹（用户需要从你这里获取最新代码）
2. **之后**：Chrome 会定期访问 `updates.xml`，发现新版本时自动弹出更新提示
3. **用户**：点击"更新"即可，无需手动下载替换

---

## 本地开发说明

### 目录结构

```
Moonton-GMHelper/（仓库根目录 = 本地开发文件夹）
├── .github/
│   └── workflows/
│       └── release.yml          ← GitHub Actions 自动化流程
├── manifest.json                ← 扩展配置（含 key 和 update_url）
├── background.js
├── content_script.js
├── panel.html
├── panel.js
├── popup.html
├── styles.css
├── commands.json
├── devtools.html
└── updates.xml
```

### 本地开发流程

1. `git clone` 克隆仓库到本地
2. 在本地修改代码
3. 修改 `manifest.json` 版本号
4. `git add .` → `git commit -m "描述"` → `git push`
5. GitHub Actions 自动触发，完成后 Pages 更新

### 本地打包（可选，用于本地测试）

项目已内置打包脚本，双击运行即可：

```
build.bat    ← Windows 打包脚本
```

运行 `build.bat`，自动将扩展文件打包为 `GMHelper.zip`，`manifest.json` 会在 zip 根目录。注意：不要手动右键压缩整个文件夹，否则 manifest.json 会嵌套在子目录导致 Chrome 无法读取。

---

## 注意事项

1. **版本号必须递增**：每次更新必须比上次大，Chrome 通过版本号判断是否需要更新
2. **只改扩展核心文件才触发 workflow**：workflow 只在 `manifest.json`、`panel.js` 等核心文件变化时触发，文档和 README 改动不会触发
3. **仓库必须 Public**：GitHub Pages 需要 Public 仓库才能开启
4. **扩展 ID 固定不变**：只要不更换 key，扩展 ID 永远为 `VasAx2Lsjsb6LetT7XJc2Q`
5. **用户首次必须手动安装**：自动更新仅对已安装的扩展有效，无法自动推送给未安装的用户

## 常见问题

**Q: workflow 运行失败怎么办？**
A: 进入仓库 **Actions** 页面，点击失败的 workflow run，查看具体报错信息，常见问题包括版本号格式错误、文件缺失等。

**Q: 更新后用户没收到提示？**
A: Chrome 检查更新间隔不固定，可能需要等待数小时。可以让用户在 `chrome://extensions/` 页面点击右上角"更新"按钮强制检查。

**Q: 可以用 Gitee Pages 代替 GitHub Pages 吗？**
A: 可以，国内访问更快。需要修改 `manifest.json` 中的 `update_url` 和 `updates.xml` 中的 `codebase` 地址，将 `github.io` 替换为 `gitee.io`。

**Q: 能修改自动更新的触发条件吗？**
A: 可以。编辑 `.github/workflows/release.yml`，修改 `on.push.paths` 列表，添加或删除需要监听的文件路径。

**Q: manifest.json 中的 key 可以更换吗？**
A: 可以更换，但更换后扩展 ID 会改变，用户需要重新安装。不建议更换。
