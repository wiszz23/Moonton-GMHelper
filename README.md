# GM助手

> 面向 Nova/Moonton 游戏内部 GM（Game Master）系统的 Chrome 浏览器扩展，提供可视化命令面板，支持快捷执行、搜索过滤、个人指令管理、历史记录及云端同步。

---

## 功能概览

| 功能 | 说明 |
|------|------|
| **内置通用指令** | 10+ 分类，约 60+ 条预置 GM 命令（装备、道具、天赋、核心、宝石、皮肤、枪械、城墙、战斗、玩家、邮件） |
| **角色 ID 批量执行** | 输入框支持空格分隔多个角色 ID，一键将命令下发到所有目标 |
| **自动执行** | 勾选后点击指令按钮会自动触发 GM 页面的"执行"按钮 |
| **拼音模糊搜索** | 内置汉字→拼音映射，支持用拼音首字母或完整拼音搜索指令名称 |
| **个人指令** | 自定义添加/编辑/删除指令，分组管理（支持创建、重命名、删除分组） |
|  **云端同步** | 个人指令通过内网后端（`10.30.138.5:3000`）自动同步，支持多设备共享 |
| **导入/导出** | 个人指令导出为 JSON 文件，支持增量导入合并 |
| **执行历史** | 记录最近 100 条执行记录，支持快速重新执行，可编辑角色 ID 后重新下发 |
| **域名白名单** | 安全机制，仅在指定的 GM 域名下注入面板；支持设置页面自定义域名 |
| **多入口** | 扩展图标弹窗 / Chrome 侧边栏 / 悬浮浮窗（注入到 GM 页面）/ DevTools 面板 |
| **快捷键** | `Ctrl+Shift+Y` 打开/聚焦助手浮窗 |
| **最小化/拖拽** | 浮窗支持最小化到右下角悬浮按钮，支持拖拽定位 |

---

## 架构

```
GMHelper/
├── manifest.json          # Chrome 扩展清单（Manifest V3）
├── background.js          # Service Worker：快捷键监听、窗口管理
├── content_script.js      # 注入脚本：读取用户名、创建浮窗容器
├── panel.html / panel.js  # 主 UI（iframe 内嵌）
├── popup.html             # 扩展图标弹窗
├── devtools.html          # DevTools 侧边面板入口
├── commands.json          # 内置 GM 指令定义（JSON，分分类组织）
└── styles.css            # 所有 UI 样式（共用）
```

### 文件职责

| 文件 | 职责 |
|------|------|
| `manifest.json` | 声明权限、入口页面、快捷键、DevTools 关联 |
| `background.js` | 监听 `Ctrl+Shift+Y` 快捷键，创建/聚焦浮窗；监听扩展图标点击 |
| `content_script.js` | 在 GM 页面注入；读取页面右上角用户名（三种策略：dropdown 首项→头像锚点→TreeWalker 兜底）；动态创建可拖拽浮窗容器；监听 storage 变化实时更新白名单 |
| `panel.html/js` | **核心 UI**：顶部 4 Tab（通用/个人/历史/设置），左侧分类导航，右侧按钮网格，Toast 通知，后端同步 |
| `popup.html` | 扩展图标点击的轻量入口（iframe 引用 panel.html） |
| `commands.json` | 预置命令，格式：`{ "分类名": [{ "name": "显示名", "text": "GM指令模板" }] }` |

---

## 内置命令分类

| 分类 | 代表指令 |
|------|---------|
| 装备 | 全套橙装/紫装、升级材料、宝箱道具 |
| 道具 | 钻石/金币/体力/能量 + 数值、经验药水、召唤券、全道具清理 |
| 天赋 | 天赋点 +100、天赋全满、天赋重置 |
| 核心 | 核心经验 +99999、核心满级、核心重置 |
| 宝石 | 宝石 +99/+999、宝石全满、宝石槽解锁 |
| 皮肤 | 皮肤全解锁、**幻形皮肤全解锁**、指定皮肤 |
| 枪械 | 武器 +999、武器满级、弹药 +999、枪械重置 |
| 城墙 | 城墙血量满、城墙满级、陷阱/箭塔/加农炮 +99 |
| 战斗 | 加血/减血、直接死亡、AI开关、升一级/一键满级、Buff/怪物 |
| 玩家 | 设置昵称/等级/VIP、解锁全功能、重置账户 |
| 邮件 | 发送系统邮件 |

> 指令模板中 `%s` 或 `%` 占位符会被自动替换为输入的角色 ID，支持多行指令。

---

## 安装

1. 打开 Chrome，地址栏输入 `chrome://extensions/`
2. 开启右上角 **开发者模式**
3. 点击 **加载已解压的扩展程序**，选择 `GMHelper` 文件夹
4. 扩展图标出现在工具栏

---

## 使用流程

### 方式一：在 GM 页面自动注入浮窗

1. 用 Chrome 打开 GM 后台页面（如 `http://gm.pre.nova.moonton.com:8201`）
2. `content_script.js` 检测到域名在白名单内后，会在页面右上角自动注入一个 **可拖拽浮窗**
3. 浮窗控制栏：标题 `GM助手`、刷新按钮、最小化 `−`、关闭 `×`
4. 双击控制栏可直接关闭面板

### 方式二：快捷键 `Ctrl+Shift+Y`

1. 在任意页面按下 `Ctrl+Shift+Y`
2. 弹出 400×700px 的独立浮窗
3. 再次按下聚焦已有窗口（若已打开）

### 方式三：扩展图标 / 侧边栏

1. 点击工具栏中的 GM助手图标 → 弹出 panel.html
2. Chrome 115+ 可右键扩展图标 → "打开侧边栏"

---

## 页面布局（panel.html）

```
┌──────────────────────────────────────────┐
│  通用   个人   历史   设置                 │  ← 顶部 Tab
├──────────────────────────────────────────┤
│  分类 │                                 │
│  ─────│  角色ID: [        ]              │
│  全部  │  搜索:   [            ]          │
│  装备  │  [✓] 自动执行      ⚙ 设置        │
│  道具  │                                 │
│  天赋  │  [装备] [道具] [天赋] [核心] ... │
│  ...   │  [宝石] [皮肤] [枪械] [城墙] ... │
│  我的  │  [战斗] [玩家] [邮件]            │
│  ─────│                                 │
│  +新建 │                                 │
└──────────────────────────────────────────┘
```

---

## 域名白名单

默认白名单（`commands.json` / `panel.js` / `content_script.js` 中各有一份）：

```
gm.pre.nova.moonton.com
gm.nova.oa.mt
gm.aoz.moontontech.net
gm-cn.yyf.muyinetwork.com
gm.jp.novagames.net
gm.usa.novagames.net
```

在 **设置 → 域名白名单** 中可自定义修改，支持子域名模糊匹配。

---

## 后端同步机制

```
后端地址: http://10.30.138.5:3000
```

| 场景 | 行为 |
|------|------|
| 通用指令首次加载 | 优先从后端 `GET /api/commands/public` 获取；若失败 fallback 到本地 `commands.json`，并写回后端 |
| 个人指令有本地数据 | 本地优先，以后端为备份目标（写后端，最多重试 3 次） |
| 个人指令本地为空 | 从后端 `GET /api/commands/{owner}` 拉取 |
| 后端同步失败 | 数据保留在 `chrome.storage.local`，下次访问时自动重试 |
| 导出 | 从后端拉取最新数据，下载为 `gm-personal-{用户名}-{时间戳}.json` |
| 导入 | 读取 JSON，增量合并（按 `name` 去重），不覆盖已有指令 |

### API 端点

| 方法 | 路径 | 说明 | 请求体 | 响应 |
|------|------|------|--------|------|
| GET | `/api/commands/public` | 获取当前公开指令列表 | — | `{ commands: "..." }`（JSON 字符串） |
| POST | `/api/commands/public` | 更新公开指令列表 | `{ commands: "..." }` | 状态码 + data |
| GET | `/api/commands/{owner}` | 获取某用户的个人指令 | — | `{ commands: "..." }` |
| POST | `/api/commands` | 保存个人指令（含 owner） | `{ owner, commands: "..." }` | 状态码 + data |

> **注意**：`commands` 字段是**字符串**（序列化后的 JSON），需双重解析：
> - 外层：`JSON.parse(responseBody)` → `{ commands: "{...}" }`
> - 内层：`JSON.parse(d.commands)` → 实际的命令对象 `{ "装备": [...], "皮肤": [...] }`

### 通用指令更新流程（Node.js 脚本参考）

```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  ① curl GET /api/commands/public                            │
│       ↓                                                      │
│  ② 保存为 current_public.json（本地备份）                       │
│       ↓                                                      │
│  ③ JSON.parse(d.commands) 解析内层嵌套结构                     │
│       ↓                                                      │
│  ④ 执行操作：去重 + 插入新指令                                 │
│       ↓                                                      │
│  ⑤ JSON.stringify(cmds) 重新序列化为字符串                     │
│       ↓                                                      │
│  ⑥ HTTP POST /api/commands/public                           │
│       ↓                                                      │
│  ⑦ 打印状态码和响应                                          │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 数据模型

```json
{
  "装备": [
    {
      "name": "全套橙装",
      "text": "add_gear %s 611210 1\nadd_gear %s 612310 1\n..."
    }
  ],
  "皮肤": [
    {
      "name": "皮肤全解锁",
      "text": "unlock_all_skin %s"
    },
    {
      "name": "幻形皮肤全解锁",
      "text": "add_item %s 145001 1\nadd_item %s 145002 1\n..."
    }
  ],
  "其他分类": [...]
}
```

- **顶层 key**（如 `"皮肤"`）：分类名称，对应面板中的左侧 Tab 分组
- **`name`**：按钮显示名称
- **`text`**：实际 GM 指令文本；支持 `\n` 分隔多条命令；`%s` / `%` 为角色 ID 占位符

---

## 个人指令管理

### 添加指令

1. 切到 **个人** Tab
2. 点击 **+ 添加指令**
3. 填写 **指令名称**（显示在按钮上）和 **指令内容**（模板，支持 `%s` 占位）
4. 选择 **所属分组**（或留空"不分组"）
5. 点击 **添加**

### 管理分组

- 左侧 **我的分组** → hover 到分组名 → 显示 ✎ 编辑按钮
- 点击分组名上方的 **+** 可新建分组
- 编辑分组弹窗中可删除该分组（指令保留，归入"不分组"）

---

## 搜索

- 通用/个人页顶部搜索框
- 支持直接输入中文指令名，或汉字对应的拼音首字母
  - 例：输入 `jj` 可匹配"加血"（`jia` → `j`）
  - 输入 `ti zhuang bei` 可匹配"全套装备"

---

## 历史记录

- 记录格式：`{ 名称, 角色ID, 指令模板, 时间, 来源Tab }`
- 最多保留 100 条（LRU，超过自动截断）
- 每条记录可编辑角色 ID 后点击 **▶ 快速执行** 重新下发
- 同名+同ID 记录会更新时间并置顶（不重复）

---

## 安全模型

| 机制 | 说明 |
|------|------|
| 域名白名单 | `content_script` 仅在白名单域名下注入；面板内部二次校验 |
| Manifest V3 | 使用 Service Worker + `chrome.storage.local`，无持久后台页 |
| 内容隔离 | 面板 UI 在 iframe 中运行，通过 `chrome.scripting.executeScript` 与 GM 页面通信 |
| 输入转义 | 角色 ID 通过空格分割后对每项做 `trim`，防止注入 |

---

## 备份工具

本地备份目录 `backup/` 用于将后端数据库快照到本地，防止误改/误删后无法恢复。

### 快速使用（Windows）

双击 `backup/backup.bat`，按菜单选择：

```
[1] 全量备份（所有用户）
[2] 增量对比备份（仅记录有变化的用户）
[3] 查看备份历史
[Q] 退出
```

### 命令行用法

```bash
# 全量备份
node backup/backup.js

# 增量对比备份（仅写入内容变化的 owner）
node backup/backup.js --diff

# 查看备份列表
node backup/list-backups.js

# 交互式恢复
node backup/restore.js

# 静默恢复最新备份全部数据
node backup/restore.js --latest

# 静默仅恢复公开指令
node backup/restore.js --latest public
```

### 备份目录结构

```
backup/
├── backup.js        # 备份脚本
├── restore.js       # 恢复脚本（交互 + 静默）
├── list-backups.js  # 查看备份历史
├── backup.bat       # Windows 快捷入口
└── backups/         # 备份数据（自动创建）
    ├── index.json            # latest 指针 + 历史列表
    └── 2026-04-08/           # 按日期归档
        ├── manifest.json     # 备份元数据（用户数/分类数/模式等）
        ├── public.json        # 公开通用指令快照
        └── 张三(Wis).json     # 各用户个人指令快照
```

详细文档见 [[GMHelper/backup/README.md]]（同一目录下）

---

## 版本历史

| 版本 | 说明 |
|------|------|
| 3.15 | 当前版本；支持 iframe 和按钮操作；增加 DevTools 入口 |
| 3.x | 重构为 MV3；新增个人指令、云端同步、导入导出；拼音搜索 |
| 早期 | MV2 版本，仅支持 popup 注入 |

---

## 开发说明

### 调试面板 UI
- 直接修改 `panel.html` / `panel.js` / `styles.css`
- 点击浮窗控制栏的 **刷新按钮** `↻` 重新加载

### 调试 content_script
- 在 GM 页面打开 Chrome DevTools（F12），Console 中搜索 `[GM助手]` 日志

### 添加新分类/指令
编辑 `commands.json`，格式：
```json
{
  "新分类": [
    { "name": "按钮显示名", "text": "gm_command %s param1 param2" }
  ]
}
```

### 修改后端地址
在 `panel.js` 和 `content_script.js` 中搜索 `BACKEND_URL`（当前：`http://10.30.138.5:3000`）

### 扩展权限说明
| 权限 | 用途 |
|------|------|
| `activeTab` | 向当前 Tab 注入脚本执行命令 |
| `scripting` | `chrome.scripting.executeScript` 调用 |
| `storage` | 本地持久化指令、历史、白名单、用户名 |
| `host_permissions: <all_urls>` | content_script 需注入到所有页面，但实际仅白名单域名生效 |
